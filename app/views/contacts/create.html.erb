<section class="container higher" id="contact">
  <% flash.each do |key, message| %>
    <p class="alert alert-<%= key %>"><%= message %></p>
  <% end %>
<br>
<h2 class="heading-first">CONTACT</h2>
      <p>Thanks for getting in touch! We will get back to you as
      soon as possible.</p>
</section>